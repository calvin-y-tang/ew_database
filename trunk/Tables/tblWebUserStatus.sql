CREATE TABLE [dbo].[tblWebUserStatus] (
    [StatusID]    INT          NOT NULL,
    [Description] VARCHAR (35) NULL,
    CONSTRAINT [PK_tblWebUserStatus] PRIMARY KEY CLUSTERED ([StatusID] ASC)
);

